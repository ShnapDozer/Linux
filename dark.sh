#!/bin/bash

Theme=$(kreadconfig5 --key LookAndFeelPackage)
if [[ "$Theme" != "org.kde.breezedark.desktop" ]]; then
theme properly. export QT_DEBUG_PLUGINS=1 export LIBGL_ALWAYS_INDIRECT=;
export DISPLAY=:0;
lookandfeeltool -a org.kde.breezedark.desktop
fi


